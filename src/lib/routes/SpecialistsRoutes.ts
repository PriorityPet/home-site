import { DiscoverRoutes } from './discoverRoutes';
export enum SpecialistsRoutesEnum {
    Specialists = DiscoverRoutes.Discover + "/specialists",
    Specialist = DiscoverRoutes.Discover + "/specialists/"
}